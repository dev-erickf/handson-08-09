https://portugol.dev/#share=gu1hu5p

programa {
  funcao inicio() {

    // Leia duas variáveis lógicas e mostre o resultado de `a ou b`(Operador OU).
    
    logico a, b, resultado

		escreva("Digite o valor de a (verdadeiro ou falso): ")
		leia(a)

		escreva("Digite o valor de b (verdadeiro ou falso): ")
		leia(b)

		resultado = a ou b

		escreva("Resultado de a ou b: ", resultado)
  }
}
